
\documentclass[preview]{standalone}
\usepackage{tikz}
\usetikzlibrary{arrows}
\usepackage{verbatim}
\def\pgfsysdriver{pgfsys-tex4ht.def}
\begin{document}

\tikzstyle{block} = [draw,minimum size=2em]
% diameter of semicircle used to indicate that two lines are not connected
\def\radius{.7mm} 
\tikzstyle{branch}=[fill,shape=circle,minimum size=3pt,inner sep=0pt]

\begin{tikzpicture}[>=latex']

    % Draw blocks, inputs and outputs
  
   \node[block] at (2,1) (block1) {$osc_1$};
    \node[block] at (2,0) (block2) {$osc_2$};
    \node[block] at (2,-1) (block3) {$osc_3$};
    \node[block] at (2,-2) (block4) {$osc_4$};
   \node[block] at (5,0) (plus) {$+$};
   \node at (8,0) (output) {$output$};
   \draw[->] (block1) -- (plus);
   \draw[->] (block2) -- (plus);
      \draw[->] (block3) -- (plus);
                    \draw[->] (block4) -- (plus);
                        \draw[->] (plus) -- (output);
\end{tikzpicture}
\end{document}